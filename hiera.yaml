---
version: 4
datadir: data
hierarchy:
    - name: 'Operating System'
      backend: yaml
      path: "os/%{facts.os.family}/%{facts.os}"

    - name: 'OS family'
      backend: yaml
      path: "os/%{facts.os.family}"

    - name: 'common'
      backend: yaml
